[package]
name = "recursec-fuzzer"
version = "0.1.0"
edition = "2021"
description = "RecurSec HTTP parameter fuzzer"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "fuzzer"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
